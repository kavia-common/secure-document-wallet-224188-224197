<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Biometrics permissions for local_auth on supported devices -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Legacy fingerprint permission for older devices (pre-Android 9) -->
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!--
      Storage & file access policy:
      - We prefer SAF-first flows using file_picker and persist files only to the
        app's sandboxed documents directory via path_provider.
      - Therefore, NO broad storage permissions (READ/WRITE_EXTERNAL_STORAGE) are declared.
      - On pre-Android 13 devices, file selection is handled by SAF/file chooser and
        does not require READ_EXTERNAL_STORAGE for our use case.
      - If a plugin ever requires additional permissions, those should be added by the plugin
        itself or revisited with minimal scope. Keeping the manifest minimal by default.
      
      Plugin notes:
      - local_auth: requires USE_BIOMETRIC (and legacy USE_FINGERPRINT) which are declared above.
      - file_picker: uses SAF and does not need storage permissions in the app manifest.
      - share_plus: shares via content URIs/intents, no extra permissions required here.
    -->

    <application
        android:label="flutter_frontend"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
