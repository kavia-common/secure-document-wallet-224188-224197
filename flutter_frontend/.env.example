# Example environment configuration for Secure Document Wallet
# Do NOT commit real secrets. Copy to .env and set values per environment.

# Optional: Feature flags
FEATURE_BIOMETRICS=true
FEATURE_SHARING=true

# Optional: App configuration
APP_NAME=Secure Document Wallet
